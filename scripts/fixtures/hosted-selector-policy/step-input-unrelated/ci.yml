name: unrelated step input
on: push
jobs:
  build:
    runs-on: ${{ fromJSON(vars.CI_LANE_TRUSTED || vars.CI_LANE_FALLBACK || '["ubuntu-24.04"]') }}
    steps:
      - uses: example/action@0123456789abcdef0123456789abcdef01234567
        with:
          runner: macos-15-documentation
