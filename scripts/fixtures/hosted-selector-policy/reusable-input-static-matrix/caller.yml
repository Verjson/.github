name: static reusable selector matrix
on: push
jobs:
  delegated:
    strategy:
      matrix:
        runner:
          - '["ubuntu-latest"]'
    uses: Verjson/.github/.github/workflows/node-ci.yml@0123456789abcdef0123456789abcdef01234567
    with:
      runner: ${{ matrix.runner }}
