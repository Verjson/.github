name: unreviewed reusable selector
on: push
jobs:
  delegated:
    uses: Verjson/.github/.github/workflows/node-ci.yml@0123456789abcdef0123456789abcdef01234567
    with:
      runner: ${{ vars.CI_RUNNER_FASTLANE || vars.CI_RUNNER_UNREVIEWED || '["ubuntu-24.04"]' }}
