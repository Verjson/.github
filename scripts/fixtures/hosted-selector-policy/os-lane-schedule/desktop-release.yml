name: scheduled desktop release
on:
  schedule:
    - cron: '0 0 * * *'
jobs:
  installer:
    runs-on: ${{ fromJSON(vars.VERJSON_LANE_TRUSTED_MACOS) }}
    timeout-minutes: 45
    steps:
      - run: npm run dist
