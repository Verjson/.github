name: push desktop release
on:
  push:
    branches: [main]
jobs:
  installer:
    runs-on: ${{ fromJSON(vars.VERJSON_LANE_TRUSTED_WINDOWS) }}
    timeout-minutes: 45
    steps:
      - run: npm run dist
