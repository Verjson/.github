name: bracket Windows lane fallback
on: workflow_dispatch
jobs:
  installer:
    runs-on: ${{ fromJSON(vars["VERJSON_LANE_TRUSTED_WINDOWS"] || vars['VERJSON_LANE_FALLBACK']) }}
    timeout-minutes: 45
    steps:
      - run: npm run dist
