# Generated by scripts/gen-authn-type-surface-required-workflow.py e1f3a897400129fc5bad6dd5182a7f20a8d920bd -- do not edit.
name: Authn required type surface

on:
  pull_request:

permissions:
  actions: read
  contents: read
  packages: read
  pull-requests: read
  statuses: read

jobs:
  type-surface:
    if: github.repository == 'Verjson/verjson-authn'
    permissions:
      actions: read
      contents: read
      packages: read
      pull-requests: read
      statuses: read
    uses: Verjson/.github/.github/workflows/node-ci-protected.yml@e1f3a897400129fc5bad6dd5182a7f20a8d920bd
    secrets:
      NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    with:
      node-version: '24'
      secretless-pr: true
      event-name: ${{ github.event_name }}
      head-repository: ${{ github.event.pull_request.head.repo.full_name }}
      head-sha: ${{ github.event.pull_request.head.sha }}
      protected-type-surface-declaration-path: .github/ci/type-surface-baseline.json
      protected-type-surface-expected-package: '@verjson/authn'
      protected-type-surface-expected-script: test:type-surface-compatibility
      approved-internal-packages: |-
        @verjson/authn
        @verjson/identity-contracts
        @verjson/tsconfig
      secretless-ci-script-plan: >-
        ["build"]
