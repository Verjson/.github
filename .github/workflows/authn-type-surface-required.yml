name: Authn required type surface

on:
  pull_request:

permissions:
  contents: read
  packages: read
  statuses: read

jobs:
  type-surface:
    if: github.repository == 'Verjson/verjson-authn'
    permissions:
      contents: read
      packages: read
      statuses: read
    uses: Verjson/.github/.github/workflows/node-ci.yml@c973a841694a41bf0b9bcd70432f64850cba0850
    secrets:
      NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    with:
      node-version: '24'
      scope: '@verjson'
      secretless-pr: true
      approved-internal-packages: |-
        @verjson/authn
        @verjson/identity-contracts
        @verjson/tsconfig
      secretless-compatibility-ranges: >-
        {"package":"@verjson/authn","ranges":["1.0.3"],"script":"test:type-surface-compatibility"}
      secretless-auxiliary-source: >-
        {"repository":"Verjson/verjson-authn","pinFile":".github/ci/type-surface-base.json","checkoutPath":".authn-type-base","sparsePath":"NEXT"}
      secretless-ci-script-plan: >-
        ["build"]
