name: probe-b-ready-only

# Throwaway probe for #468. Delete with the scratch branch.
on:
  pull_request:
    types: [ready_for_review]

jobs:
  observe:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "probe-b action=${{ github.event.action }}"
